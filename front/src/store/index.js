import { createStore } from 'vuex'
import request from './module/request.module'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    request
  }
})
